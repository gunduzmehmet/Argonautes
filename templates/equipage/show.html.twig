{% extends 'base.html.twig' %}

{% block title %}Equipage{% endblock %}

{% block body %}
    <h1>Equipage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipage.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ equipage.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipage_index') }}">Retour </a>

    <a href="{{ path('equipage_edit', {'id': equipage.id}) }}">Modifier</a>

    {{ include('equipage/_delete_form.html.twig') }}
{% endblock %}
